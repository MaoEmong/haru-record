# Project Instructions

## Documentation

- Store all project documentation in the `.docs/` directory.
- Use `.docs/` for project notes, specifications, design decisions, setup guides, and implementation plans.
- Do not create new documentation files outside `.docs/` unless the user explicitly requests it.
